apiVersion: batch/v1
kind: Job
metadata:
  name: mkimotsu-jigsaw-compexp-orchestrator
spec:
  backoffLimit: 0
  template:
    spec:
      restartPolicy: Never
      nodeSelector:
        nvidia.com/gpu.product: "NVIDIA-GeForce-RTX-3090"
      volumes:
        - name: workspace
          persistentVolumeClaim:
            claimName: mkimotsu-jigsaw-qlora-pvc
      containers:
        - name: jigsaw-compexp-orchestrator
          image: pytorch/pytorch:2.2.2-cuda12.1-cudnn8-runtime
          resources:
            requests:
              cpu: "8"
              memory: "64Gi"
              nvidia.com/gpu: 1
            limits:
              cpu: "8"
              memory: "64Gi"
              nvidia.com/gpu: 1
          volumeMounts:
            - name: workspace
              mountPath: /workspace
          env:
            - name: HF_TOKEN
              valueFrom:
                secretKeyRef:
                  name: mkimotsu-secrets
                  key: hf_token
            - name: KAGGLE_USERNAME
              valueFrom:
                secretKeyRef:
                  name: mkimotsu-secrets
                  key: kaggle_username
            - name: KAGGLE_KEY
              valueFrom:
                secretKeyRef:
                  name: mkimotsu-secrets
                  key: kaggle_key
            - name: PYTHONPATH
              value: /workspace/repo
          command: ["/bin/bash", "-c"]
          args:
            - |
              set -euxo pipefail
              cd /workspace

              pip install -U "numpy<2" tensorboard
              pip install -U \
                "transformers==4.44.2" \
                "huggingface_hub==0.25.2" \
                "datasets==2.18.0" \
                "accelerate==0.33.0" \
                "evaluate==0.4.1" \
                "pandas" \
                "iterative-stratification" \
                "scikit-learn" \
                "spacy"

              python -m spacy download en_core_web_sm

              mkdir -p /workspace/logs

              python /workspace/repo/compexp_toxicity/compexp/orchestrator.py \
                --model-path /workspace/sequence_classification/saved_model \
                --output-dir /workspace/compexp_outputs_full \
                --batch-size 1 \
                2>&1 | tee /workspace/logs/compexp_orchestrator_$(date +%Y%m%d_%H%M%S).log
